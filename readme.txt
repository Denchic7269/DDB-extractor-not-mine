dotnet run ddbextractor.cs path_to_file
path_to_file being the path to the file, including the file's name and extension, for instance, "C:\some folder\another folder\vocaloidname.ddb"
You'll need .NET Core, might need the SDK? 